# Lazy aliases for common commands
alias cls="clear"

# Make neofetch use the custom logo
alias neofetch="neofetch --source $DOTFILES_DIR/configs/neofetch/logo.txt --ascii_colors 213"

# Dotfile aliases
alias dotdir="cd $DOTFILES_DIR"
alias dotsync="$DOTFILES_DIR/bootstrap.sh -f"
